import math

x = math.pi/4  # Set x to pi/4 radians
expression = "math.cos(x)"  # Create a string representation of the expression you want to evaluate
result = eval(expression)  # Use eval() to evaluate the expression

print(result)  # Output the result of evaluating the expression
